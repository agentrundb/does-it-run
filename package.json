{
  "name": "does-it-run",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "does-it-run — run AI agents against standard test cases and publish reproducible evidence. The fact layer behind agentrundb.com.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "verify": "node runner/run.js",
    "verify:dry": "node runner/run.js --dry-run",
    "verify:claude-code": "node runner/run.js --agent claude-code"
  }
}
